import React from 'react';

export default function Logout() {
  return (
    <div
      className="Live"
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '400px',
        fontSize: '25px',
      }}
    >
      <h1>Logout</h1>
    </div>
  );
}
